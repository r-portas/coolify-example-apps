services:
  host-network:
    image: python:3.12-alpine
    network_mode: host
    restart: unless-stopped
    working_dir: /srv
    volumes:
      - ./public:/srv:ro
    command: python -m http.server 8888
