services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    environment:
      - DUMMY_TEST_VAR=1
    volumes:
      - uptime-kuma-data:/app/data
    ports:
      - "3001:3001"

volumes:
  uptime-kuma-data: